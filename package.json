{
  "name": "secure-cicd-app",
  "version": "1.0.0",
  "description": "Sample app for CI/CD pipeline project",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "test":    "jest --coverage",
    "test:ci": "jest --coverage --ci"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest":      "^29.5.0",
    "supertest": "^6.3.3"
  }
}
